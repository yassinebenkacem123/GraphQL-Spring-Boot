spring:
  application:
    name: graphQL

  graphql:
    graphiql:
      enabled: true
  datasource:
    url: jdbc:mysql://localhost:3306/storeDB
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true



